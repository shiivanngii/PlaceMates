{
  "name": "placemates",
  "version": "0.1.0",
  "private": true,
  "description": "PlaceMates — campus placement companion",
  "scripts": {
    "dev": "concurrently --names \"web,api\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "install:all": "cd frontend && npm install && cd ../backend && npm install",
    "lint": "npm run lint --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
